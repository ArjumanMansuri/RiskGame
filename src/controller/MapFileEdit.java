package controller;

public class MapFileEdit {
	public String fileExists(String command) {
		// if file does not exist create a new blank .map file
		return "fileName";
	}
	public String commandParser(String command) {
		// During map edit phase, the map file validity would be checked only on the invocation of the validatemap command.
		// when user wants to save the mapfile, check validity of the map and return appropriate output.
		return "";
	}
}
